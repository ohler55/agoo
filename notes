;; -*- mode: outline; outline-regexp: " *[-\+]"; indent-tabs-mode: nil; fill-column: 120 -*-

- refs
  - http://www.rubydoc.info/github/rack/rack/master/file/SPEC

rdoc -t Agoo -m README.md -x "test/*" -x "example/*" -x extconf.rb

- todo

- con connection array
 - change to double linked list?
  - still need way of determining pa size
   - could just make a large size or maybe use max socket from os, allocate on heap is better
   - setrlimit to hard limit from getrlimit
   
